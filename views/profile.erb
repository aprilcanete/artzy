
<h2>Your Gallery</h2>

<section class="arts">

    <% arts.each do |art| %>
        <img src="<%= art["image_url"] %>" alt="<%= art["name"] %>" >
        <h2>
            <a href="/arts/<%= art["id"] %>"><%= art["name"] %></a>
        </h2>
    <% end %> 

</section>